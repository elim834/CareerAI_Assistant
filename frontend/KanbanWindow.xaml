<Window x:Class="frontend.KanbanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kanban Board" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="5">
            <TextBlock Text="New" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
            <ListBox x:Name="NewList" AllowDrop="True"
                     Drop="Column_Drop" DragEnter="Column_DragEnter"
                     MouseMove="ListBox_MouseMove"
                     PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                     Tag="new" MinHeight="450"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="5">
            <TextBlock Text="Applied" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
            <ListBox x:Name="AppliedList" AllowDrop="True"
                     Drop="Column_Drop" DragEnter="Column_DragEnter"
                     MouseMove="ListBox_MouseMove"
                     PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                     Tag="applied" MinHeight="450"/>
        </StackPanel>

        <StackPanel Grid.Column="2" Margin="5">
            <TextBlock Text="Accepted" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
            <ListBox x:Name="AcceptedList" AllowDrop="True"
                     Drop="Column_Drop" DragEnter="Column_DragEnter"
                     MouseMove="ListBox_MouseMove"
                     PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                     Tag="accepted" MinHeight="450"/>
        </StackPanel>

        <StackPanel Grid.Column="3" Margin="5">
            <TextBlock Text="Rejected" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
            <ListBox x:Name="RejectedList" AllowDrop="True"
                     Drop="Column_Drop" DragEnter="Column_DragEnter"
                     MouseMove="ListBox_MouseMove"
                     PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                     Tag="rejected" MinHeight="450"/>
        </StackPanel>
    </Grid>
</Window>